# Minishell
Developed a Linux Mini Shell in C supporting built-in and external commands using fork(), execvp(), and waitpid(). Implemented job control commands (jobs, fg, bg), background process execution, signal handling, and special commands (echo $$, echo $?, echo $SHELL).Used a singly linked list for process management with modular and well-documented code
